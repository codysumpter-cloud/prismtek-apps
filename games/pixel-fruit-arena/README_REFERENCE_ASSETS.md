# Reference Assets

Reference assets only. Not for release.

The uploaded GIF files are temporary local development/testing assets only. They may be used for animation timing, frame layout, and prototype testing under `assets/reference/onepiece-test/`.

Do not ship, distribute, commit as permanent artwork, or include these files in public or release builds. `npm run build` removes `assets/reference` from `dist`.
