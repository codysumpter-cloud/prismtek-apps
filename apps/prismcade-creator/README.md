# Prismcade Creator MVP

Tiny manifest-first creator prototype.

## Run

From the repo root:

```bash
python -m http.server 4173
```

Open:

```txt
http://localhost:4173/apps/prismcade-creator/
```

The tool lets a creator choose a template, enter metadata, preview a Prismcade manifest, and copy/download the JSON.
